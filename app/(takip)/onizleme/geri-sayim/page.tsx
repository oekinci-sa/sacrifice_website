import TakipHomeLaunchCountdown from "@/app/(takip)/components/takip-home-launch-countdown";

/**
 * Önizleme: homepage_mode = geri_sayim (yakında / fiyat listesi)
 */
export default function OnizlemeGeriSayimPage() {
  return <TakipHomeLaunchCountdown />;
}
