import TakipContent from "@/app/(takip)/(takip)/page-takip";

/**
 * Önizleme: homepage_mode = follow_up / takip (kesim takip ekranı)
 */
export default function OnizlemeTakipPage() {
  return <TakipContent />;
}
